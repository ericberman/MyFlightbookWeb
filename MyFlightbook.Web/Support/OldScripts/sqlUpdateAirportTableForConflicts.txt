ALTER TABLE `logbook`.`airports` 
DROP PRIMARY KEY, ADD PRIMARY KEY (`AirportID`, `Type`) ;

/* Convert lat/lon to doubles */
ALTER TABLE `logbook`.`airports` ADD COLUMN `Latitude` DOUBLE NOT NULL AFTER `SourceUserName`,
ADD COLUMN `Longitude` DOUBLE NOT NULL AFTER `Latitude`;
update airports a set a.Latitude=cast(a.Lat as decimal(14,10)), a.Longitude=cast(a.long as decimal(14,10));

ALTER TABLE `logbook`.`airports` DROP COLUMN `Long` , DROP COLUMN `Lat` ;

ALTER TABLE `logbook`.`airports` ADD COLUMN `Preferred` INT NULL DEFAULT 0 COMMENT 'Used for disambiguation when two airports share the same lat/lon'  AFTER `Longitude` ;

