CREATE TABLE `logbook`.`passwordresetrequests` (
  `ID` VARCHAR(36) NOT NULL COMMENT 'Unique ID for the request\n',
  `username` VARCHAR(255) NULL COMMENT 'Username for whom the request was made\n',
  `expiration` DATETIME NULL COMMENT 'Timestamp for the request to expire\n',
  `status` INT NULL COMMENT 'Status (= 0 pending or unused, 1 = successful reset, 2 = expired, 3 = failed reset)\n',
  PRIMARY KEY (`ID`))
COMMENT = 'Contains records of pending or past password reset requests';
ALTER TABLE `logbook`.`passwordresetrequests` 
ADD UNIQUE INDEX `ID_UNIQUE` (`ID` ASC);