delimiter $$

CREATE TABLE `flightvideos` (
  `idFlightVideos` int(11) NOT NULL AUTO_INCREMENT,
  `idFlight` int(10) unsigned NOT NULL COMMENT 'The id of the object (flight, typically) to which the video is associated',
  `vidRef` varchar(255) NOT NULL COMMENT 'The data (URL or HTML code) which references the video.',
  `Comment` varchar(255) DEFAULT NULL COMMENT 'Comment for the video',
  PRIMARY KEY (`idFlightVideos`),
  KEY `idFlightForVideo` (`idFlight`),
  CONSTRAINT `idFlightForVideo` FOREIGN KEY (`idFlight`) REFERENCES `flights` (`idFlight`) ON DELETE CASCADE ON UPDATE NO ACTION
) ENGINE=InnoDB AUTO_INCREMENT=14 DEFAULT CHARSET=utf8 COMMENT='Links to videos for a given flight'$$

