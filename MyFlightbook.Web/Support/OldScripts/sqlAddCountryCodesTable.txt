CREATE TABLE `logbook`.`CountryCodes` (
  `ID` INTEGER UNSIGNED NOT NULL AUTO_INCREMENT,
  `Prefix` VARCHAR(10) NOT NULL DEFAULT '',
  `CountryName` VARCHAR(80) NOT NULL DEFAULT '',
  PRIMARY KEY(`ID`)
)
ENGINE = InnoDB
COMMENT = 'Aircraft Tailnumber prefixes, by country';

ALTER TABLE `logbook`.`countrycodes` ADD COLUMN `locale` VARCHAR(3) NOT NULL COMMENT '2 letter ISO code for the locale (helps decide which one to use)'  AFTER `Prefix` ;
