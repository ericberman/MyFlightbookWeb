delimiter $$

CREATE TABLE `pendingvideos` (
  `jobID` varchar(255) NOT NULL COMMENT 'Amazon AWS Elastic Transcoder job ID - assigned by Amazon',
  `guid` varchar(255) NOT NULL COMMENT 'Guid (filename) for the MP4 file',
  `Comment` varchar(255) NOT NULL COMMENT 'Comment provided by the user.',
  `Submitted` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT 'Timestamp of when job was submitted',
  `virtpathID` int(11) NOT NULL COMMENT 'The virtual pathID for this (typically 0 for flights)',
  `imageKey` varchar(255) NOT NULL COMMENT 'The key for the pending video',
  `bucket` varchar(255) NOT NULL COMMENT 'The bucket in which the pending video resides',
  PRIMARY KEY (`jobID`),
  UNIQUE KEY `guid_UNIQUE` (`guid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='Videos awaiting reply from Amazon Elastic Transcoder'$$

